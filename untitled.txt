<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kenangan Yang Tak Pernah Hilang... | Galau Abadi</title>
    <link href="https://fonts.googleapis.com/css2?family=Amatic+SC:wght@400;700&family=Poppins:wght@300;400;600&family=Caveat:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(-45deg, #1a1a2e, #16213e, #0f3460, #533483);
            background-size: 400% 400%;
            animation: galaxy 20s ease infinite;
            color: #e0e0e0;
            overflow-x: hidden;
            min-height: 100vh;
        }

        @keyframes galaxy {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Stars Background */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle 2s infinite;
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.2); }
        }

        /* Rain Effect */
        .rain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 2;
        }

        .drop {
            position: absolute;
            width: 2px;
            height: 50px;
            background: linear-gradient(to bottom, transparent, rgba(173,216,230,0.8));
            animation: fall linear infinite;
        }

        @keyframes fall {
            0% {
                transform: translateY(-50px);
                opacity: 1;
            }
            100% {
                transform: translateY(100vh);
                opacity: 0;
            }
        }

        /* Header */
        .header {
            text-align: center;
            padding: 3rem 1rem 1rem;
            position: relative;
            z-index: 10;
        }

        .logo {
            font-family: 'Amatic SC', cursive;
            font-size: 4rem;
            color: #ffd700;
            text-shadow: 0 0 20px rgba(255,215,0,0.5);
            margin-bottom: 1rem;
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 20px #ffd700, 0 0 30px #ffd700, 0 0 40px #ffd700; }
            to { text-shadow: 0 0 10px #ffd700, 0 0 20px #ffd700, 0 0 30px #ffd700; }
        }

        /* Container */
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem;
            position: relative;
            z-index: 10;
        }

        /* Diary Entry */
        .diary {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            border-radius: 25px;
            padding: 3rem;
            margin-bottom: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px rgba(0,0,0,0.3);
            animation: fadeInUp 1.5s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .date {
            font-family: 'Caveat', cursive;
            font-size: 1.5rem;
            color: #b0b0b0;
            margin-bottom: 1rem;
            text-align: right;
        }

        .galau-emoji {
            font-size: 5rem;
            text-align: center;
            margin: 2rem 0;
        }

        .title-galau {
            font-family: 'Amatic SC', cursive;
            font-size: 3.5rem;
            color: #ff6b6b;
            text-align: center;
            margin: 2rem 0;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .story {
            font-size: 1.3rem;
            line-height: 1.9;
            margin: 2rem 0;
            text-align: justify;
            color: #d0d0d0;
        }

        .story strong {
            color: #ffd700;
        }

        /* Memories Grid */
        .memories {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin: 3rem 0;
        }

        .memory-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 20px;
            padding: 2rem;
            border-left: 5px solid #ff6b6b;
            transition: all 0.3s;
            cursor: pointer;
        }

        .memory-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.15);
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }

        .memory-emoji {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .memory-title {
            font-size: 1.4rem;
            color: #ffd700;
            margin-bottom: 1rem;
            font-weight: 600;
        }

        /* Quote */
        .quote-box {
            background: linear-gradient(135deg, rgba(255,107,107,0.2), rgba(255,215,0,0.2));
            padding: 2.5rem;
            border-radius: 20px;
            text-align: center;
            margin: 3rem 0;
            border: 1px solid rgba(255,215,0,0.3);
        }

        .quote {
            font-family: 'Caveat', cursive;
            font-size: 2rem;
            color: #ffd700;
            margin-bottom: 1rem;
            font-style: italic;
        }

        .quote-author {
            color: #ff6b6b;
            font-weight: 600;
        }

        /* Action Buttons */
        .actions {
            text-align: center;
            margin: 3rem 0;
        }

        .btn {
            display: inline-block;
            padding: 1rem 2.5rem;
            margin: 0.5rem;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
        }

        .btn-danger {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
        }

        .btn-warning {
            background: linear-gradient(45deg, #ffd700, #ffed4e);
            color: #333;
        }

        .btn-success {
            background: linear-gradient(45deg, #00b894, #00cec9);
            color: white;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.3);
        }

        /* Typewriter Effect */
        .typewriter {
            overflow: hidden;
            border-right: 3px solid #ffd700;
            white-space: nowrap;
            animation: typing 4s steps(40, end), blink-caret 0.75s step-end infinite;
        }

        @keyframes typing {
            from { width: 0; }
            to { width: 100%; }
        }

        @keyframes blink-caret {
            from, to { border-color: transparent; }
            50% { border-color: #ffd700; }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .logo { font-size: 2.5rem; }
            .title-galau { font-size: 2.5rem; }
            .diary { padding: 2rem 1.5rem; }
            .story { font-size: 1.1rem; }
        }
    </style>
</head>
<body>
    <!-- Stars -->
    <div class="stars" id="stars"></div>
    
    <!-- Rain -->
    <div class="rain" id="rain"></div>

    <!-- Header -->
    <div class="header">
        <h1 class="logo">🌙 Manusia kuat🌙</h1>
        <p style="font-size: 1.3rem; opacity: 0.8;">Kenangan yang tak pernah hilang...</p>
    </div>

    <!-- Container -->
    <div class="container">
        <!-- Diary Entry -->
        <div class="diary">
            <div class="date" id="currentDate"></div>
            <div class="galau-emoji">😔💔️</div>
            <h2 class="title-galau">Aku Masih di Sini...</h2>
            
            <div class="story typewriter" id="storyText">
                Setiap malam aku masih membuka galeri foto kita. Senyummu yang dulu selalu menghiasi hari-hariku kini hanya tinggal kenangan. Aku mencoba move on, tapi hati ini bandel sekali. <strong>Kamu masih mengisi 99% pikiranku.</strong> Bahkan lagu-lagu yang kita dengerin bareng masih kuputar diam-diam. Aku tahu harusnya lupain, tapi... aku tak bisa
            </div>
        </div>

        <!-- Memories -->
        <div class="memories">
            <div class="memory-card" onclick="showMemory('Foto pertama kita di taman itu masih jadi wallpaper HP ku... ')">
                <div class="memory-emoji">📸</div>
                <div class="memory-title">Foto Pertama</div>
                <p style="color: #b0b0b0;">Masih jadi wallpaper...</p>
            </div>
            <div class="memory-card" onclick="showMemory('Chat terakhir kita masih belum aku hapus. Kadang aku baca ulang tengah malam... ')">
                <div class="memory-emoji">💬</div>
                <div class="memory-title">Chat Lama</div>
                <p style="color: #b0b0b0;">Belum terhapus...</p>
            </div>
            <div class="memory-card" onclick="showMemory('Baju yang kamu tinggalkan masih kukenakan diam-diam. Wanginya masih tercium... ')">
                <div class="memory-emoji">👕</div>
                <div class="memory-title">Baju Kamu</div>
                <p style="color: #b0b0b0;">Masih kukenakan...</p>
            </div>
            <div class="memory-card" onclick="showMemory('Lagu 'Cinta Tak Harus Memiliki' masih jadi alarm pagiku... 🎵')">
                <div class="memory-emoji">🎵</div>
                <div class="memory-title">Lagu Kita</div>
                <p style="color: #b0b0b0;">Alarm pagiku...</p>
            </div>
        </div>

        <!-- Quote -->
        <div class="quote-box">
            <div class="quote">"Terkadang, yang paling sulit dilupakan bukanlah orangnya, tapi kenangan indah yang pernah kita ciptakan bersamanya."</div>
            <div class="quote-author">— Pria Galau Abadi —</div>
        </div>

        <!-- Actions -->
        <div class="actions">
            <button class="btn btn-danger" onclick="playSadSong()">
                <i class="fas fa-music"></i> Putar Lagu Galau
            </button>
            <button class="btn btn-warning" onclick="writeDiary()">
                <i class="fas fa-pen"></i> Tulis Diary
            </button>
            <button class="btn btn-success" onclick="tryMoveOn()">
                <i class="fas fa-forward"></i> Coba Move On
            </button>
        </div>
    </div>

    <!-- Memory Modal -->
    <div id="memoryModal" style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 100; justify-content: center; align-items: center;">
        <div style="background: rgba(255,255,255,0.1); backdrop-filter: blur(20px); padding: 3rem; border-radius: 20px; max-width: 500px; text-align: center;">
            <div id="memoryContent" style="font-size: 1.5rem; margin-bottom: 2rem; color: #ffd700;"></div>
            <button class="btn btn-danger" onclick="closeMemory()" style="width: 200px;">Tutup <i class="fas fa-times"></i></button>
        </div>
    </div>

    <script>
        // Create stars
        function createStars() {
            const starsContainer = document.getElementById('stars');
            for(let i = 0; i < 100; i++) {
                const star = document.createElement('div');
                star.className = 'star';
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.width = star.style.height = (Math.random() * 3 + 1) + 'px';
                star.style.animationDelay = Math.random() * 2 + 's';
                starsContainer.appendChild(star);
            }
        }

        // Create rain
        function createRain() {
            const rainContainer = document.getElementById('rain');
            for(let i = 0; i < 100; i++) {
                const drop = document.createElement('div');
                drop.className = 'drop';
                drop.style.left = Math.random() * 100 + '%';
                drop.style.animationDuration = (Math.random() * 0.5 + 0.5) + 's';
                drop.style.animationDelay = Math.random() * 2 + 's';
                rainContainer.appendChild(drop);
            }
        }

        // Set current date
        document.getElementById('currentDate').textContent = new Date().toLocaleDateString('id-ID', {
            weekday: 'long',
            year: 'numeric',
            month: 'long',
            day: 'numeric'
        }) + ' - Jam ' + new Date().toLocaleTimeString('id-ID');

        // Memory modal
        function showMemory(content) {
            document.getElementById('memoryContent').textContent = content;
            document.getElementById('memoryModal').style.display = 'flex';
        }

        function closeMemory() {
            document.getElementById('memoryModal').style.display = 'none';
        }

        // Buttons functions
        function playSadSong() {
            const songs = [
                "🎵 'Cinta Tak Harus Memiliki' - ST12",
                "🎵 'Kenangan Manis' - Naff",
                "🎵 'Hilang' - Maudy Ayunda",
                "🎵 'Sakit Hati' - Geisha"
            ];
            alert(songs[Math.floor(Math.random() * songs.length)]);
        }

        function writeDiary() {
            const diary = prompt('Tulis perasaanmu sekarang...');
            if(diary) {
                alert('Diary tersimpan di hati... \n"' + diary + '"');
            }
        }

        function tryMoveOn() {
            setTimeout(() => {
                alert('...Gagal. Masih ingat senyummu. ');
            }, 2000);
        }

        // Initialize
        window.addEventListener('load', () => {
            createStars();
            createRain();
        });

        // Update time every second
        setInterval(() => {
            const now = new Date();
            document.getElementById('currentDate').textContent = now.toLocaleDateString('id-ID', {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: 'numeric'
            }) + ' - Jam ' + now.toLocaleTimeString('id-ID');
        }, 1000);
    </script>
</body>
</html>